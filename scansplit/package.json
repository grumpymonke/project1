
{
  "name": "scansplit-project",
  "version": "1.0.0",
  "description": "Root package for managing backend and frontend in the Scansplit project.",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "author": "Your Name",
  "license": "MIT"
}
